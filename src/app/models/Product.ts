import { Byte } from "@angular/compiler/src/util"

export interface Product {
    id : Number,
    name : string,
    description : string,
    price : Number,
    addedOn : Date,
    image : string

}